// Роздруковуємо числа від 3 до 20
for (let i = 3; i <= 20; i++) {
    console.log(i);
}

// Роздруковуємо числа від 10 до 20
for (let i = 10; i <= 20; i++) {
    console.log(i);
}

// Просимо ввести 2 числа
let startNum = parseInt(prompt("Введіть початкове число:"));
let endNum = parseInt(prompt("Введіть кінцеве число:"));

// Роздруковуємо числа між введеними числами
for (let i = startNum; i <= endNum; i++) {
    console.log(i);
}